properties:
  variables:
    type: array
    items:
      type: object
      properties:
        name:
          type: string
        type:
          type: string
        description:
          type: string
        value:
          type: [string, number, boolean]
        variables:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              type:
                type: string
              description:
                type: string
              value:
                type: [string, number, boolean]
  commonVariables:
    type: array
    items:
      type: string